/**
 * @type {Map<string, any>}
 */
const kv = new Map(); // TODO serialize-deserialize hot ?
kv.set("number", 42);
kv.set("title", "🧑‍🚀 Hello, Astronaut!");
kv.set("changeme", "Change this text !");
kv.set("__rtf_welcome", "");

export default kv;
